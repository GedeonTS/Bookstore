import React from "react";
import  ReactDOM  from "react";
import App from "./App";

ReactDOM.render(
    <React.StrictMode>
        <App />
    </React.StrictMode>, document.querySelector('#root'));

